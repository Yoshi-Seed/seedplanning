# Codex協調開発 指示書（アンケート画面簡易システム向け）

## 1. 目的
この指示書は、複数のCodexが同時に作業しても競合を最小化し、最後に1つの成果物として統合（オーケストレーション）できるようにするための標準ルールです。

対象は「アンケート画面が作れる簡易システム」です。

- ユーザー（非エンジニア）が操作しやすいこと
- 実装方針が統一されていること
- 各担当の成果を確実に結合できること

を重視します。

---

## 2. 開発原則（全Codex共通）

1. **場当たり的な実装を禁止**
   - 先に仕様・API・データ契約を定義してから実装する。
2. **再現可能性を担保**
   - すべての変更は、手順・テスト結果・影響範囲を記録する。
3. **疎結合**
   - 画面、ロジック、データ、テストを分離し、担当ごとの依存を減らす。
4. **最終統合前提**
   - 変更は必ず「受け入れ条件（DoD）」を満たした状態で引き渡す。
5. **説明責任**
   - 非エンジニア向けの説明（何を、なぜ、どう確認したか）を必ず添える。

---

## 3. 役割分担（推奨）

### Codex-A: 要件・仕様定義
- 画面要件（必須入力、設問形式、送信条件）を定義
- 受け入れ条件（Acceptance Criteria）を明文化
- API契約（リクエスト/レスポンス）を定義

**成果物**
- `docs/spec/requirements.md`
- `docs/spec/api-contract.md`
- `docs/spec/acceptance-criteria.md`

### Codex-B: フロントエンド（UI）
- アンケート作成画面・回答画面のUIを実装
- バリデーション表示、操作導線、アクセシビリティ対応

**成果物**
- UI実装ファイル
- 画面操作手順
- UIテスト結果

### Codex-C: バックエンド（データ処理）
- アンケート保存・回答保存のロジック実装
- バリデーションのサーバー側担保
- エラーハンドリングとログ方針

**成果物**
- API実装ファイル
- 単体テスト結果
- エラーケース検証結果

### Codex-D: QA/統合オーケストレーション
- 各担当成果の整合性確認
- 統合テスト・回帰確認
- リリース可否判定

**成果物**
- `docs/reports/integration-report.md`
- `docs/reports/test-matrix.md`
- リリースノート（非エンジニア向け）

---

## 4. 共通インターフェース契約（先に固定）
統合時の衝突を防ぐため、最初に以下を固定します。

1. **設問データ構造（例）**
   - `id`
   - `type`（single_choice / multiple_choice / text / rating）
   - `title`
   - `required`
   - `options[]`（選択式のみ）

2. **回答データ構造（例）**
   - `surveyId`
   - `respondentId`（匿名ならセッションID）
   - `answers[]`（questionId + value）
   - `submittedAt`

3. **エラー形式（例）**
   - `code`
   - `message`
   - `field`（該当フィールドがある場合）

> 仕様変更が必要な場合は、実装前にCodex-Aが仕様を更新し、全担当に通知してから着手します。

---

## 5. ブランチ運用ルール

- メインブランチ: `main`
- 担当ブランチ例:
  - `feature/spec-foundation`
  - `feature/ui-survey-builder`
  - `feature/api-survey-service`
  - `feature/integration-orchestration`

### コミット規約（推奨）
- `feat(ui): add survey question builder form`
- `feat(api): implement survey response endpoint`
- `test(ui): add validation tests for required fields`
- `docs(spec): define survey answer schema`

### PR規約
- 1PR 1目的
- PR本文に以下を必須記載
  1. 目的
  2. 変更点
  3. テスト内容・結果
  4. 影響範囲
  5. 残課題

---

## 6. Definition of Done（完了条件）
各Codexのタスク完了には次を必須とします。

1. 仕様と差分がない（差分がある場合は仕様更新済み）
2. テストが成功している
3. 例外系を最低1つ以上検証している
4. 非エンジニア向け説明がある
5. 引き継ぎメモ（次担当への注意点）がある

---

## 7. テスト戦略（最小セット）

### 単体テスト
- 設問バリデーション
- 必須チェック
- 保存ロジック

### 結合テスト
- 「アンケート作成 → 公開 → 回答 → 保存」
- 異常系（必須未入力、存在しない設問ID、形式不一致）

### 画面確認
- 必須エラー表示
- 送信成功時の導線
- モバイル表示崩れ確認

### 回帰テスト
- 既存ページに影響がないこと

---

## 8. オーケストレーション手順（最終統合）

1. **成果物収集**
   - 各CodexのPRから変更点・テスト結果を一覧化
2. **仕様整合チェック**
   - API契約、データ構造、UI文言を突合
3. **段階マージ**
   - `spec` → `api` → `ui` → `integration`の順で統合
4. **統合テスト実施**
   - テストマトリクスに沿って全ケース実施
5. **非エンジニア向け報告作成**
   - できること・できないこと・次の改善案を平易に記載

---

## 9. 作業報告テンプレート（各Codex必須）
以下テンプレートで、最終的にオーケストレーターが統合します。

```md
## 作業概要
- 目的:
- 実施内容:

## 変更ファイル
- path/to/file1
- path/to/file2

## テスト
- 実行コマンド:
- 結果:
- 補足:

## 非エンジニア向け説明
- 何ができるようになったか:
- 使い方:
- 注意点:

## 引き継ぎ事項
- 次担当が確認すべき点:
- 未対応事項:
```

---

## 10. 初回スプリント推奨タスク

1. Codex-A: 最小仕様（MVP）確定
2. Codex-B: 回答画面（1ページ）を先に実装
3. Codex-C: 回答保存API（インメモリ可）を実装
4. Codex-D: E2E最小1本（正常系）を作成

この順に進めることで、早い段階で「実際に使える縦スライス」を確認できます。

---

## 11. 非エンジニア向け最終報告フォーマット

- 今回できること
- 操作手順（1. 2. 3. で記載）
- 失敗しやすい操作と対処法
- 今回見送った機能
- 次回改善予定

専門用語は避け、画面名ベースで説明してください。

---

## 12. 禁止事項

- 仕様未確定のまま実装開始
- テスト未実施での引き渡し
- 他担当の責務領域への無断変更
- 変更理由の記録なしコミット

以上を守ることで、複数Codexでも品質を維持したまま短時間で統合可能になります。
